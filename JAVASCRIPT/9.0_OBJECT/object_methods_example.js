const person = { name: "pari", age: 20 };

console.log(Object.keys(person));   // ["name", "age"]
console.log(Object.values(person)); // ["pari", 20]
console.log(Object.entries(person)); // [["name", "pari"], ["age", 20]]
